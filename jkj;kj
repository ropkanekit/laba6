#include <iostream>
#include <iomanip>
#define _USE_MATH_DEFINES 
#include <math.h> 
using namespace std;

int main()
{
 
    
        Y = pow(cos(start), 2) + log10(start) + 2;
        cout << "Result(x, Y) : " << start << setw(10) << Y << endl;
        start+=step;
        
    
}
